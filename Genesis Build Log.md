---
status: living document
part-of: "[[Design Notes]]"
updated: 2026-08-25 (small cleanup batch + GitHub remote)
---

# Genesis Build Log

Genesis is the working, vibe-coded prototype of Cam's title/escrow production software — a real, click-through application, not a mockup. This note tracks what's actually built, how it's put together, and a running change log so a new session/thread can pick up context fast. Design *decisions* (the "why") live in [[Design Notes]], [[Design Notes - Platform]], and [[Design Notes - Curative & Tasking]] — this note is the "what's actually built and where" companion.

## Live build

**https://claude.ai/code/artifact/dfa56b55-9198-4247-94e3-813b2a158f78**

Open that link any time to use the current prototype. It's a published Claude Artifact — private to Cam unless shared, persists across sessions, and is updated in place (same URL) every time a change is published.

## What it is, technically

- A single self-contained HTML file (`genesis-app.html`) — a vanilla-JavaScript single-page application. No build step, no framework, no server. Everything (styles, markup, logic) lives in one file.
- All screens render via template-string functions that write to `innerHTML`. No component framework.
- Data persists to the browser's `localStorage` (key `genesis_orders_v1`) — each order/file is a JSON object in an array. This is per-browser storage: it lives in whichever browser tab/profile has the Artifact open, not in a shared database. There is no multi-user sync, no login system, and no server-side data store yet — that's expected at this prototyping stage, not a bug.
- Published and updated via the Artifact tool — republishing the same file path keeps the same URL, so the link above never changes even as the app evolves.

### A few load-bearing implementation patterns (useful context for future sessions)

- **`blankOrder()` vs. `normalizeOrder()`:** `blankOrder()` defines the full shape of a brand-new order; `normalizeOrder()` backfills that same shape onto orders already saved in `localStorage` (run once at app startup). Every new field has to be added to *both* — `newOrder()` calls `blankOrder()` directly, not `normalizeOrder()`. Missing this has caused at least one new-order crash (fixed).
- **Live preview refresh:** most text fields just call `save()` on change without a full re-render. Any field that feeds a computed preview box on the same screen (e.g. the Deed's Generated Summary) has a small dedicated `refreshXSummary()` function that patches just that one DOM node, called from the field's change handler.
- **List CRUD pattern:** every persisted list (Requirements, Exceptions, Security Instruments, Liens, Exception Matters, Related Documents, Affidavits, Deed Subject To, Signature Lines, etc.) uses the same add / pencil-icon inline edit / trash-icon delete pattern, with a `recordRow()` helper and a per-list `state.editXId` toggle. Nothing hard-deletes silently in a Final-stage record — see the Draft/Final and disposition patterns below.
- **Seed-chip pattern:** "From this file" chips generate pre-composed text from underlying structured data elsewhere in the file (e.g. Requirements chips from Security Instruments/Liens, Deed Subject To chips from Exception Matters). A chip dedups via a `sourceType:sourceId` tag on the target record, disappearing once used and reappearing if the target is deleted.
- **Draft/Final lifecycle pattern:** used on the Commitment and the Deed — a boolean `final` + `finalizedAt` timestamp, confirm-before-finalize UI, and confirm-before-revert-to-draft UI. Reverting a Final record back to Draft preserves any data already entered (e.g. Recording Data), just hides it again. Commitment's revert is blocked while a Clear to Close is issued.
- **Independent-copy-with-refill pattern:** party/text fields that need to be independently editable but start from a computable default (Derivation's Grantor/Grantee, Deed's Grantor/Grantee/Legal Description/Parcel/Derivation/Situs Address, Security Instrument's Mortgagor/Mortgagee) are auto-filled once (non-destructive fill-if-blank), then fully decoupled from their source — with an explicit "Copy from a file contact" or "Refill from source" button to re-pull on demand rather than silently re-syncing. As of 2026-08-24 this is now the pattern for every Doc Prep party/text field, per Cam's "all fields need to be editable" direction — there's no longer a live-pull/read-only category on these screens. The Notary Acknowledgement screen is the one partial exception: each signer's acknowledgment *text* is independently editable this same way, but the *list* of who needs an entry still tracks live from Contacts (adding/removing a signer role updates the list; it doesn't touch already-written text).

## Testing practice

Every code change is tested with Playwright (a headless-browser scripting tool) before it's published — the app is exercised the way a user would, and console/page errors are checked. As of 2026-08-25, test scripts live checked into the project itself at `tests/smoke_test_*.js` (one per feature area) and run through a single consolidated runner, `tests/run-all.js` (`node tests/run-all.js`, or `--filter=<substring>` for one area) — concurrency-limited, prints one pass/fail summary instead of each script needing to be run and eyeballed by hand, and exits non-zero on any failure so it can gate a publish. 8 tests that had gone stale (testing UI shapes superseded by later redesigns, or pure debug dumps with no real assertions) were archived to `tests/legacy/` rather than deleted, with a README explaining why each one. Before publishing any change, the full suite gets re-run so a new feature can't silently break an old one.

**Version control:** as of 2026-08-25, `genesis-app.html` is a real git repo (branch `master`) with commit history — no more single-file-with-no-rollback-point. A real remote is now configured too (`https://github.com/campennydarden-cpu/M-L-Title.git`, branch `main`) — see the git-remote change-log entry below for how pushes actually happen (the cloud build workspace can't push to GitHub directly; Cam pushes from his own Mac Terminal using the session's git-bundle export as the transport).

## Screens built so far

**General:** Order Entry, Order Information, Contacts, Property (Identification / Legal Description / Plat & Survey Matters sub-tabs).

**Title:** Prelim Title Search (Derivation, Security Instruments + Related Documents, Liens & Encumbrances, Exception Matters), Commitment Sch A (policy/coverage, Chain of Title, plus Commitment Form Type/Number/Revision and Transaction ID Data), Commitment Sch B-I/B-II (Requirements/Exceptions with Draft/Final + reversible Finalize, now with the real ALTA baked-in standard items and gap-exception toggle ahead of file-specific ones), Curative (disposition workflow, Clear to Close with Issue/Rescind). The generated Commitment document now carries the real ALTA jacket boilerplate (Notice, Commitment to Issue Policy, Commitment Conditions) — see the 2026-08-25 entry below — rather than being data-only.

**Document Preparation** (its own flat nav group): Deed (full spec build — see below), Security Instrument, Affidavits, Power of Attorney, Notary Acknowledgement.

**Escrow / Closing** (its own flat nav group): Settlement Type, Options, Additional Title/Escrow Charges, Tax Prorations, Other Prorations, CDF Pages 1-5, Express HUD Pages 1-3 (standard 1991 HUD-1). CDF Pages 1-5 now visually recreate the real Closing Disclosure's look (black section bars, numbered line rows, grouped columns) with refi-mode branching off Loan Purpose; HUD stays on the plain card style. See below for architecture notes.

**Toolbar (available from any screen):** Requested Tasks, Checklist Tasks, Attachments (Commitment version snapshots), File History (full audit trail).

**Not yet built:** Premiums/Endorsements (placeholders only), Policy screens, Escrow Register/trust accounting/batch disbursements, Scheduling, Doc Tree/state-specific document text, Title Plant/Subdivision module, Agency Setup/admin, workflow builder (automation rules), any of the out-of-the-box integrations. All of these are designed in [[Design Notes - Platform]] but not coded yet.

## Recent change log (most recent first)

**2026-08-25 — Small cleanup batch + real GitHub remote (Option 3 of the approved sequence).** Same continuation session as Options 1 and 2 below. Four items: (1) **Browser tab title** fixed from the stale "Genesis Commitment Builder" to "Genesis — M&L Title & Escrow," matching the sidebar's own brand markup. (2) **Seeded demo order**, addressing the audit's Outsider finding that a cold empty app looks unfinished to a first-time viewer: `buildDemoOrder()` builds one realistic fictional file (2 married buyers, a seller, a lender, a settlement agent — no SSN/DOB on anyone — with a finalized Commitment and Clear to Close already issued) and `seedDemoOrderIfFirstEverLoad()` adds it exactly once, guarded by its own `genesis_demo_seeded_v1` flag (separate from the orders array) so it seeds only on a truly empty first-ever load, never reappears after the demo file is deleted, and never touches a profile that already has real orders. This collided with all 43 existing Playwright test files' `localStorage.clear()` + reload "simulate a fresh browser" convention, which is otherwise indistinguishable from a genuine first-time user — fixed by having every other test file's fixture also mark the demo as pre-seeded, restoring the exact established regression baseline (852/854 passing, same 2 pre-existing unrelated failures) while adding 9 new demo-seed assertions. (3) **Real GitHub remote**: `https://github.com/campennydarden-cpu/M-L-Title.git`, branch `main`. The cloud build workspace's own sandbox categorically blocks `git push` to external hosts (confirmed with two different approaches, including one with no credential visible in the command at all) — so Cam pushes from his own Mac Terminal instead, using the same git-bundle file this project already exports to his "Genesis Build" folder each session as the transport, plus a GitHub fine-grained personal access token he generated and was walked through step by step. Caught and fixed a real gotcha in this workflow: a bundle synced earlier in a session goes stale the moment a new commit lands, so it has to be re-exported and re-synced after every commit if a mid-session GitHub push is possible — an early push in this session landed one commit behind until this was caught via file-mtime comparison and corrected with a fetch+force-push. (4) **SSN/DOB handling decision** (flagged by the audit as Cam's call, not a build decision to make unprompted): Cam's answer was to leave SSN/DOB stored in plaintext with no access control for now, and revisit it once a user-permissions system exists to actually restrict who can see them — "Not there yet." Committed (`03975c7`), bundle re-synced, published live.

**2026-08-25 — Silent data-loss fix (Option 1 of the audit's approved sequence).** Continuation session, same day as the five-persona audit rerun below. Cam approved a 3-step sequence from the audit's Executor punch list — fix data loss first, then a First Principles build-order conversation, then a small cleanup batch — and said to roll through it. This entry covers step 1. Rewrote `save()`/`load()`: the old empty `catch(e){}` (silent write failures) is replaced with a visible save-status banner (error/warn states, a "Back Up Data" action); a corrupted/unparseable `localStorage` value on load is now preserved as-is plus copied to a timestamped `_corrupt_backup_*` key, instead of the old behavior of immediately calling `save()` with a reset-to-`[]` state and overwriting the only copy of the corrupted data. New Backup & Restore modal: export renders the full order array as copyable JSON text (Copy button, clipboard-API with an execCommand fallback), restore accepts pasted JSON and upserts by id — updates matching orders, adds new ones, never deletes anything absent from the pasted set. This is a copy/paste flow rather than a file download because the published Artifact's sandbox makes `<a download>`/`data:`/`blob:` downloads and script-driven saves inert. 14 new Playwright assertions (banner visibility on simulated write failure, corrupted-load preservation + backup-key creation, modal open/close, restore upsert semantics) — 843/845 total passing, same 2 pre-existing unrelated Trustee-derivation-clause failures as the established baseline. Committed to git (`29d2929`), bundle re-synced to the Mac, published to the live Artifact. Next: had the First Principles sequencing conversation (Option 2) the same session — see below — then moved into the small cleanup batch (Option 3): title tag, seeded demo order, real git remote, SSN/DOB handling decision.

**2026-08-25 — First Principles sequencing decision (Option 2 of the approved sequence).** Asked Cam directly, per the audit's own framing: turn the next build phase toward Escrow Register/trust accounting (the roadmap's own named highest-license-risk component, still 100% design-only) or keep polishing what's already built (Doc Prep, CDF/HUD, Curative)? Cam's decision: **stay on polishing/hardening for now, don't start trust accounting yet.** Stated reasoning — he wants the hierarchy, logic, and architecture nailed down before expanding into a new domain; a lot of time already spent this build has gone into fixing things broken by downstream consequences that weren't considered upfront, and he wants that reduced before trust accounting (much higher stakes if something's wrong) gets added on top. This is a deliberate call, not the roadmap-inversion happening by default — see [[Open Items & Parking Lot]] for the same note.

**2026-08-25 — Vesting-clause fix, git + consolidated tests, CDF K/L/M/N structuring, five-persona audit.** Cam asked what to improve next based on notes/memory; picked all four surfaced options (vesting-clause bug, git, consolidated test runner, one more CDF gap), then specified the CDF gap as structuring K/L/M/N as real fields rather than free-text Other Items — the item flagged as deferred in the 2026-08-25 CDF recreation entry above. **Vesting-clause bug** (flagged 2026-08-24, unfixed until now): the Contacts list was calling each contact's own `clauseForContact()` in isolation, which is correct for a single record but doesn't match how every generated document actually renders a married pair (`groupVestingClause`/`marriedPairAmong`, both already correct) — so a married individual's row read "a married person" instead of the combined "X and Y, husband and wife" the real documents use. Fixed with a new `contactListClause()` helper that mirrors the document-generation logic for the list preview. **Git:** initialized a real repo at the project root (branch `master`), `.gitignore` added; two commits so far. **Still open:** no remote configured — commits exist only in the cloud build workspace. **CDF K/L/M/N:** grounded in the actual CFPB blank CD form (read the real PDF page rather than working from memory) before implementing. Sale Price, Loan Amount, Deposit, Seller Credit, and the two mortgage payoffs are now structured fixed fields instead of freeform "Other Items" entries; new `paidInAdvance` flag on Tax/Other Prorations routes each proration into the correct CD adjustment section (paid-in-advance → K/M, unpaid/arrears → L/N, matching the real form's two proration-adjustment blocks). Sale Price/Loan Amount auto-flow from Order Entry's Purchase Price/Loan Amount live (previously Page 1's copies only synced at file creation or via a manual "Refill" button, which would have left K/L stale against Order Entry after any later edit — caught and fixed via test regression, not by design upfront). **Test runner + suite:** see Testing practice section above. Full regression: 829/831 assertions passing across 41 files; the 2 remaining failures (`smoke_test_derivation.js`, Trustee derivation clause) are pre-existing, confirmed via git-stash comparison to predate this session's changes entirely — not touched. **Five-persona audit:** separately, Cam asked for a comprehensive "counsel of five" audit (Contrarian/First Principles/Expansionist/Outsider/Executor personas) of the whole app, all design docs, and all memory, plus competitive research against SoftPro, Qualia, ResWare and other industry-standard title/escrow platforms — see the synthesis delivered in chat and [[Open Items & Parking Lot]] for the resulting punch list. **Publish status:** committed to git; publish to the live Artifact URL blocked this session by a sandbox network restriction (couldn't read the current live version before overwriting it) — pending Cam enabling `*.frame.claudeusercontent.com` under Code → Network access, or another retry.

**2026-08-25 — CDF recreation + refi-mode pass.** Continuing the same-day CDF/CD gap-closing work Cam prioritized after last session's document research (deferred behind the Commitment jacket, see the entry below), Cam asked mid-session for a bigger change than originally scoped: make the CDF screens visually recreate the real Closing Disclosure's look (black section bars, numbered line rows, Borrower/Seller and At-Closing/Before-Closing grid columns) rather than Genesis's usual generic card-and-field-grid style, with Genesis's normal editable inputs laid directly over that chrome. Confirmed scope via two quick questions: apply it to all 5 CDF pages now (not just the ones being touched this pass), and build it as a styled recreation rather than a pixel-exact PDF overlay. New shared CSS/JS "CD-recreation chrome" (`cdBar`, `cdMeta`, `tplCdfLineItemTable`, `cdCalcRow` helpers) now renders all 5 CDF pages; HUD is untouched, still the plain card style. Landed the 4 CDF data gaps in the same pass: Loan Amount added to Page 1 (was missing entirely, auto-fills from Order Entry); Page 2 restored the Borrower/Seller × At-Closing/Before-Closing column split with computed D/I/J subtotal rows; Page 3 gained a real "Calculating Cash to Close" table (Loan Estimate vs. Final, 9 rows) above the existing K/L/M/N — LE is always manual entry (no source for it in Genesis), Final computes live where Page 2/3 already has the number and stays manual where it doesn't; and refi-mode branching driven by Page 1's Loan Purpose (Cam's earlier call, not a new toggle) — Page 2 drops Seller-Paid columns, Page 3 swaps K/L/M/N for a single Payoffs and Payments table plus a refi-specific Calculating Cash to Close variant, Page 5 drops the two Real Estate Broker cards. Refi Cash to Close math (Loan Amount − At-Closing Closing Costs − Total Payoffs and Payments) checked directly against the numbers in CFPB's own refi CD example, not just eyeballed. Migrated existing files' Page 2 line items (old `borrowerAmount`/`sellerAmount` → new `borrowerAtClosing`/`sellerAtClosing` + `borrowerBeforeClosing`/`sellerBeforeClosing`) and Page 3's old flat LE/Final numbers into the new `calc` object. 25+ new Playwright assertions (CD-recreation rendering, Loan Amount auto-fill, At/Before split + D/I/J subtotals, Calculating Cash to Close LE/Final behavior, full refi-mode branching across Pages 2/3/5, migration backfill) plus a full regression sweep — found and fixed one pre-existing latent test bug in `smoke_test_alta_jacket.js` (a substring check for "Owner's Policy" false-positived off the Loan Policy card's own ALTA-Form dropdown, which always renders that option text in the DOM regardless of selection; not a real app bug, just a bad assertion, now checks the field ID instead) and two stale numbering/label assertions from the ALTA jacket work below (already fixed then); no new regressions from today's change. Published. Not yet built: HUD's equivalent recreation styling (explicitly deferred), Page 1's multi-period Projected Payments breakout (ARM/step loans), and structuring K/L/M/N's fixed line items (Sale Price, Deposit, tax-proration rows, etc.) as dedicated fields instead of generic free-text rows.

**2026-08-25 — ALTA Commitment jacket boilerplate.** Cam uploaded 10 reference docs (blank ALTA Commitment + Short Form Commitment forms, blank/annotated CDs, CD refi examples) and asked Genesis's Commitment and CDF screens be checked against them. Two research passes (one per doc set) found real gaps in both; Cam prioritized via three quick questions: Commitment jacket first (over CDF fixes), the ALTA form text verbatim (over paraphrased/summarized), and CDF refi-mode branching should derive from Loan Purpose when that work starts later. This entry covers the Commitment jacket piece only — CDF/CD fixes remain deferred. Added, covering both the ALTA Commitment for Title Insurance (2016) and the ALTA Short Form Commitment for a Short Form Residential Loan Policy (2017): a Commitment Form Type selector on Schedule A (blank/Standard/Short Form — blank behaves as Standard); new Schedule A fields (Commitment Number, Revision Number, Company's State of Organization, Requirements Time Period, a Transaction Identification Data card, and — Short Form only — ALTA 8.1-06 Environmental Protection Lien endorsement statutes); Short Form's structural constraints enforced as real UI gating (Owner's Policy card hidden/Loan Policy forced regardless of Order Entry's Policy Type, Estate/Interest fixed to "Fee Simple" instead of free text). Schedule B-I/B-II now always show the ALTA-standard baked-in items (4 Requirements for Standard, 5 for Short Form which adds a tax-payment item; a toggleable standard "gap" Exception) ahead of file-specific ones, not editable/removable — `computeReqLabels` gained a numbering offset so the Commitment tab, Curative's disposition workflow, and the generated document all agree on the same item numbers. The generated Commitment document gained the verbatim Notice section, Commitment to Issue Policy (fills the two real blanks — state of organization, requirement time period — and branches Standard's full paragraph vs. Short Form's incorporation-by-reference wording), and, Standard form only, the full text of Commitment Conditions 1-9 with all lettered sub-items (Short Form incorporates them by reference instead, per the real form). Clear to Close deliberately excludes this jacket boilerplate (`includeJacket:false` on its shared `renderDocBody` call) since it's an escrow-facing Schedule B summary, not a reissued commitment. **Flagged to Cam:** this is verbatim copyrighted ALTA form text, restricted by the forms' own footer to ALTA licensees/members in good standing — confirm M&L's status before this goes out on an actual issued commitment. 30 new Playwright assertions (legacy migration backfill, Schedule A field save/round-trip, Standard-vs-Short-Form gating, standard-item rendering + gap-exception toggle, generated-doc jacket content, cross-screen numbering consistency) plus a full regression sweep across the existing suite — found and fixed 2 stale assertions in older tests that hardcoded pre-jacket numbering/labels (`smoke_test_schedule_a.js`'s "Date/Time Issued" → "Commitment Date" rename, `smoke_test_related_doc_subitems.js`'s item numbers shifting from 1/1a/1b/2 to 5/5a/5b/6 now that 4 standard items are baked in ahead of file-specific ones); no other regressions found. (The pre-existing married-couple joint-vesting-clause bug and a batch of test scripts stale from before Item L, unrelated to this change, remain as previously flagged — not touched.) Published.

**2026-08-25 — Escrow / Closing, first scaffold pass.** Per Cam's request to build out the whole Escrow/Closing area at once, added a new flat nav group with 13 tabs: Settlement Type, Options, Additional Title/Escrow Charges, Tax Prorations, Other Prorations, CDF Pages 1-5, Express HUD Pages 1-3. Built per Cam's explicit "scaffold everything first" call (over fully building one screen before starting the next) — this is a first pass, not field-level-final. Settlement Type (CDF/HUD) gates which page-family's nav tabs show (hidden, not deleted, so nothing already entered is lost by switching); left unset, both show. New architecture, introduced here specifically because CDF Pages 2-3 and HUD Pages 1-2 needed ~22 near-identical itemized-list-with-computed-total sections: a generic `tplLineItemTable`/`wireLineItemTable` component (description + configurable $ columns, computed total) instead of hand-writing each one — a deliberate departure from the per-feature CRUD pattern used everywhere else in the app, justified purely by the repetition count. Real computed "flow-down" values (not stored fields): CDF Page 1's Total Closing Costs sums Page 2; Cash to Close is Page 3's K−L/M−N; HUD's Cash at Settlement and 1400 Total work the same way from HUD Pages 1-2. CDF Page 1 (Borrower/Seller/Lender names, Sale Price) and CDF Page 5 / HUD Page 1 (Lender, Settlement Agent contact blocks) use the established independent-copy-with-refill pattern, auto-filled once from Contacts/Order Entry. New per-diem proration calculator (365-day year, Seller responsible through the day before closing, Buyer owns closing day — flagged to Cam as a convention, not universal) powers both Tax Prorations (County/City/HOA-COA/Other, HOA/COA staying under Tax per Cam's call) and a new Other Prorations screen (non-tax items, no type dropdown). Scope calls made this pass, flagged to Cam as first-pass and not yet confirmed: CDF/HUD Page 2 itemized columns simplified to Borrower/Seller/Paid-by-Others (CDF) or Borrower/Seller (HUD), dropping the federal forms' At-Closing/Before-Closing timing split; CDF Page 4 kept to a single notes field (lender-owned boilerplate, per Cam's explicit choice); CDF Page 5's Mortgage Broker/Real Estate Broker blocks are free text (not new Contact roles); HUD built as the standard 3-page 1991 HUD-1 only (not 1986 or HUD-1A). `o.escrow` is a new top-level order object with a `normalizeOrder()` backfill for existing files. 30 new Playwright assertions (nav gating, legacy migration, auto-fill/independent-copy behavior, proration math, line-item CRUD + computed totals, CDF/HUD flow-down math) plus the full regression suite — regression turned up several pre-existing stale test scripts referencing tab/field names from before earlier nav restructures (not caused by this change, not fixed, out of scope). Published.

**2026-08-24 — Contact-level Signature Line(s) configuration.** Buyer/Borrower and Seller Contact Detail screens gained an editable "Signature Line(s)" section, per Cam's direct request. Lives in the Contact's own draft-edit form alongside the existing Principals list — add/edit/delete freely, saved together with the rest of the contact. Auto-fills once with a single computed default line (reuses the existing `signatureLineForContact()` logic — name for Individual, POA-aware; combined entity+principals line for LLC/Corp/Partnership/Trust), with an explicit "Regenerate default from entity details" button to recompute on demand. Persisted as `contact.signatureLines: [{id, text}]`, empty for non-vesting roles. Deliberately not wired into Deed's own (separate, independent) Signature Lines list — see [[Design Notes]]. 16 new Playwright assertions plus the full regression suite, all passing.

**2026-08-24 — Doc Prep full-editability pass + Security Instrument Note fields.** Per Cam's direction that all Doc Prep fields need to be editable ("filter down where applicable, but edited for the final merged document"): Deed's Legal Description/Parcel/Derivation/Situs Address converted from read-only computed displays to independently-editable fields, auto-filled once, each with a "Refill from source" button. Security Instrument's Mortgagor/Mortgagee reversed from live-pulled-from-Contacts to the same independent-copy pattern as Deed's Grantor/Grantee (name, entity type, principals roster, "Copy from a file contact" incl. married-pair "Both"). Notary Acknowledgement's per-signer text converted to an independently-editable, persisted record with a Regenerate button — the signer list itself stays live-filtered from Contacts. Power of Attorney's Attorney-in-Fact Name is now editable directly on this screen (writes to the same Contact field — not an independent copy, since POA's architecture already ties recording data to the Contact record); Principal Name intentionally left non-editable from this screen since renaming it would rename the underlying Contact. Added a new Security Instrument "Note" card (Note Date, Note Amount, Maturity Date, Interest Rate) — Note Date/Amount live-mirror Dated Date/Loan Amount until independently edited. 40+ new Playwright assertions plus the full regression suite, all passing (one pre-existing, unrelated bug noticed during regression: the married-couple joint vesting clause on the Contacts list sometimes renders each spouse separately instead of combining — not touched, flagged for a future session).

**2026-08-24 — Deed screen address/situs detail.** Prepared By and Return To previews now show the selected contact's name plus their best address on file (Prepared By: general address; Return To: prefers Forwarding Address). A Return To role-group with contacts on differing addresses shows the first one with a note that they differ. Situs Address's computed default now includes City/State/Zip (preferring the Property screen's own fields, falling back to Order Entry's), not just the street.

**2026-08-24 — Deed screen independence pass.** Reversed the earlier design that made Deed's Grantor/Grantee read-only and live-pulled from Contacts: they're now independently editable (name, entity type, principals roster), auto-filled once from Contacts, then fully decoupled — with a "Copy from a file contact" convenience including a married-pair "Both" option. Return To now supports role-group selection ("All Seller," "All Buyer/Borrower," generalized to any role on the file), not just a single contact. Signature Lines converted from a read-only computed list to a real CRUD list (seed-from-Grantor + free-form add/edit/delete). Notary Block converted from a read-only display synced to the standalone Notary Acknowledgement screen into an independently editable textarea with an explicit Generate button, no longer synced. 44 new Playwright assertions plus the full regression suite, all passing.

**2026-08-24 — Doc Prep nav restructure + Deed screen full rebuild.** Moved Document Preparation to its own flat top-level nav group (5 tabs: Deed, Security Instrument, Affidavits, Power of Attorney, Notary Acknowledgement), replacing the earlier nested-subtab pattern. Rebuilt the Deed screen field-by-field to Cam's exact spec (see [[Design Notes - Platform]]'s Doc Prep section for the full field order), including a Draft/Final lifecycle with Recording Data fields gated on Final.

**2026-08-24 — Commitment Un-finalize.** Added a reversible "Revert to Draft" to the Commitment, mirroring the existing Finalize flow, blocked while a Clear to Close is issued.

**2026-08-24 — Schedule A Coverage Amount auto-fill.** Purchase Price → Owner's Policy Coverage Amount, Loan Amount → Loan Policy Coverage Amount, same non-destructive fill-if-blank pattern used elsewhere.

**2026-08-24 — Doc Prep initial build.** First version of the 5 Document Preparation screens (Deed, Security Instrument, Affidavits, Power of Attorney, Notary Acknowledgement), initially as nested sub-tabs (later restructured, see above).

*(Earlier sessions built Order Entry, Contacts, Property, Prelim Search, Commitment/Schedule A/B, Curative/CTC, the toolbar sections, and the Related Document sub-item requirements feature — see [[Design Notes]] and [[Design Notes - Curative & Tasking]] for what's in each.)*

## Where things stand / picking this up

If you're starting a new session and want to keep building Genesis: open the live build link above to see current state, skim this note's change log for the most recent work, and check [[Open Items & Parking Lot]] for anything flagged but not yet closed out. The three Design Notes files are the "why" behind what's built — read the one relevant to whatever you're changing next.
